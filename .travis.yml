sudo: false
dist: trusty
language: cpp

addons:
  apt:
    packages:
     - gfortran
     - libhdf5-dev
     - libgsl0-dev
     - build-essential

install: ./install.sh

before_script: git lfs pull

script: ./run data/eos/1 data/eosls220_low.h5 --test 3353 523079


